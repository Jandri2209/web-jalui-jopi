---
layout: base.njk
title: Cómo llegar
lang: es
permalink: /como-llegar/
alt: {"en": "/en/getting-here/", "fr": "/fr/comment-arriver/"}
---

{% set addr = (site.address | replace("·"," ") | trim) %}
{% set q = addr | urlencode %}
{% set pid = site.placeId %}

{% if pid %}
  {% set mapsHref = "https://www.google.com/maps/search/?api=1&query=" ~ q ~ "&query_place_id=" ~ pid %}
  {% set writeReviewHref = "https://search.google.com/local/writereview?placeid=" ~ pid %}
  {% set mapsIframe = "https://www.google.com/maps?output=embed&q=place_id:" ~ pid %}
{% else %}
  {% set mapsHref = "https://www.google.com/maps/search/?api=1&query=" ~ q %}
  {% set writeReviewHref = mapsHref %}
  {% set mapsIframe = "https://www.google.com/maps?output=embed&q=" ~ q %}
{% endif %}

<section class="section">
  <h1>{{ i18n.es.HowToGetHere }}</h1>

  <div class="gethere-grid">
    <div class="card contact-box">
      <h3>{{ i18n.es.Address }}</h3>
      <p>{{ site.address }}</p>

      <div class="cta-row">
        <a class="btn" href="tel:+34948840496">📞 {{ i18n.es.Call }}</a>
        <a class="btn alt" href="mailto:{{ site.email }}">✉ {{ i18n.es.Write }}</a>
      </div>

      <a class="btn wide" href="{{ mapsHref }}" target="_blank" rel="noopener">
        {{ i18n.es.OpenInMaps or "Abrir en Google Maps" }}
      </a>
    </div>

    <div class="map-card">
      <div class="map-embed">
        <iframe
          src="{{ site.mapsEmbed or mapsIframe }}"
          loading="lazy"
          allowfullscreen
          referrerpolicy="no-referrer-when-downgrade"
          title="Mapa — {{ site.address }}"></iframe>
      </div>
      <p class="map-note">{{ i18n.es.MapNote or "Amplía el mapa para ver accesos y aparcamiento." }}</p>
    </div>
  </div>
</section>
