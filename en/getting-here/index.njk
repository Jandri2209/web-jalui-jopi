---
layout: base.njk
title: Getting here
lang: en
permalink: /en/getting-here/
alt: {"es": "/como-llegar/", "fr": "/fr/comment-arriver/"}
---

{% set addr = (site.address | replace("·"," ") | trim) %}
{% set q = addr | urlencode %}
{% set pid = site.placeId %}

{% if pid %}
  {% set mapsHref = "https://www.google.com/maps/search/?api=1&query=" ~ q ~ "&query_place_id=" ~ pid %}
  {% set writeReviewHref = "https://search.google.com/local/writereview?placeid=" ~ pid %}
{% else %}
  {% set mapsHref = "https://www.google.com/maps/search/?api=1&query=" ~ q %}
  {% set writeReviewHref = mapsHref %}
{% endif %}
{% set mapsIframe = "https://www.google.com/maps?output=embed&q=" ~ q %}

<section class="section">
  <h1>{{ i18n.en.HowToGetHere }}</h1>

  <div class="gethere-grid">
    <div class="card contact-box">
      <h3>{{ i18n.en.Address }}</h3>
      <p>{{ site.address }}</p>

      <div class="cta-row">
        <a class="btn" href="tel:+34948840496">📞 {{ i18n.en.Call }}</a>
        <a class="btn alt" href="mailto:{{ site.email }}">✉ {{ i18n.en.Write }}</a>
      </div>

      <a class="btn wide" href="{{ mapsHref }}" target="_blank" rel="noopener">
        {{ i18n.en.OpenInMaps or "Abrir en Google Maps" }}
      </a>
    </div>

    <div class="map-card">
      <div class="map-embed">
        <iframe
          src="{{ site.mapsEmbed or mapsIframe }}"
          loading="lazy"
          allowfullscreen
          referrerpolicy="no-referrer-when-downgrade"
          title="Mapa — {{ site.address }}"></iframe>
      </div>
      <p class="map-note">{{ i18n.en.MapNote or "Amplía el mapa para ver accesos y aparcamiento." }}</p>
    </div>
  </div>
</section>