{
  "name": "bar-jalui-jopi",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy && npx puppeteer browsers install chrome --cache-dir=$PUPPETEER_CACHE_DIR && node scripts/generate-pdfs.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.15"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0",
    "puppeteer": "^23.7.3"
  },
  "engines": { "node": ">=18" }
}
