{
  "name": "bar-jalui-jopi",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy && node scripts/generate-pdfs.js",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "nodemailer": "^6.9.15",
    "puppeteer": "^23.7.3"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0"
  },
  "engines": { "node": ">=18" }
}
